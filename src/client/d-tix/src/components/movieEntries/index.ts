import { Component, Prop, Vue } from 'vue-property-decorator';
import Base from "@/utils/Base";

@Component
export default class MovieEntries extends Base {
    created(): void {
    }

    destroyed(): void {
    }

    mounted(): void {
    }

}